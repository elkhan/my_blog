const personOne = {
  firstName: 'Jake',
}
const { firstName } = personOne
console.log(firstName) // Jake
// The same as
const personTwo = {
  lastName: 'Doe',
}
const { lastName } = personTwo
console.log(lastName) // Doe
